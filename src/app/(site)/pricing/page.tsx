import { PricingPageContent } from "@/module/billing/billing.customer";

export const metadata = {
  title: "Pricing",
  description: "Choose a monthly plan and manage credits, payments, and subscriptions.",
};

export default function PricingPage() {
  return <PricingPageContent />;
}
